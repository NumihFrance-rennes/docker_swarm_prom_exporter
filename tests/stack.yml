version: "3.8"

services:
  replicated:
    image: alpine:latest
    command: sleep infinity
    deploy:
      mode: replicated
      replicas: 2

  global:
    image: alpine:latest
    command: sleep infinity
    deploy:
      mode: global
